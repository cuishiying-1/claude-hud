# v0.7 模型能力同步实施计划（真实上下文窗口 + 真实多币种价格）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 HUD 使用用户真实模型的上下文窗口（DeepSeek 1M 而非 200k 兜底）与真实双币种价格（¥/$ 语言感知），机制全走表：打包内置表 + 手动 `model sync` 从 GitHub 拉注册表 + 用户手写覆盖。

**Architecture:** ① 新 `ModelEntry`（窗口 + synced_at + 双币种 PriceEntry）进 `[models]` 配置段；② `builtin_models()` 在 pricing.rs 与既有 `builtin_pricing()` 同层扩展；③ `resolve_context_window` 单点解析（窗口链 [models] → 内置 → stdin，不同则重算 pct 钳制 ≤100），在 compact/dashboard/serve 三个管线入口调用；④ `price_for(lang)` 语言选币种（链：`[pricing]` → `[models]` 该币种 → 内置该币种 → 透传），`merged_pricing` 改走 price_for，`currency()` 决议符号（显式 → zh ¥ → $）替换全部 12 处 `config.currency_symbol` 消费点；⑤ 新 `src/core/modelsync.rs`：ureq 拉 `registry/models.toml`（HUD_REGISTRY_URL 测试钩子）→ 解析校验（失败 exit 1 不写）→ toml::Value 手术合并进 config.toml `[models]`（保留用户条目 + synced_at）→ 交互询问 env（可注入 prompt 闭包）；⑥ cc_config 增 env 块读写（CLAUDE_CODE_MAX_CONTEXT_TOKENS）；⑦ clap `model sync/env/list` 命令组 + doctor 校验 + serve 币种透传。

**Tech Stack:** Rust 2021 · serde/toml 0.8 · ureq 2 · tiny_http 0.12（测试）· ratatui · i18n（locales/en|zh.toml 点分 key）

**用户约束**（本仓库硬性规则）：不自动 `git add/commit/push`（用户手动提交，批次末 AskUserQuestion 授权，不带 Co-Authored-By）；不运行 `cargo fmt`；cargo 不在 PATH（命令统一前缀 `export PATH="$HOME/.cargo/bin:$PATH" &&`）；不 stage 未跟踪的 `fixtures/`、`reports/`；构建用 `cargo build`（黑盒 harness 从 target/debug/claude-hud.exe 解析）。

---

## 事实基线（本批次全部已读源码验证）

1. **pricing.rs**：`PriceEntry {input, output, cache_read, cache_creation}` 全 serde default（pricing.rs:12-22）；`builtin_pricing()` 9 个 Claude 模型（input×0.1/×1.25 推 cache，pricing.rs:30-54）；`merged_pricing(config)` = 内置表 + 用户 `[pricing]` 覆盖（pricing.rs:57-63）；`inject_cost`/`inject_cost_realtime` 注入 `currency_symbol` 用 `config.currency_symbol.clone()`（pricing.rs:164、189）；pricing.rs 已 `use super::config::AppConfig`、`use super::session::SessionData`（pricing.rs:5-6）。
2. **config.rs**：`currency_symbol: String`（serde default "$"，config.rs:43-44，`default_currency_symbol()` 57-59）；手动 `Default` 在 387 行 `currency_symbol: "$".into()`；`language: String` + `language()` 方法（返回 i18n::Language）；config.rs **未导入** i18n（新增方法需加 `use crate::core::i18n::Language;`）；`[pricing]: HashMap<String, PriceEntry>`（config.rs:50）。
3. **compact.rs**：`render()` 99-108 行 parse stdin 后调 `run_pipeline(&data, ...)`（data 非 mut，本批次改 mut + resolve）；`run_pipeline` 内 alert/预算/压缩通知全部消费 `data.context_window`（148、167 行传 `&config.currency_symbol` 给 notify）。
4. **dashboard.rs**：`run_loop` 中 `let mut data = initial.snapshot.to_session_if_fresh(...)`（79 行）+ 刷新点 `if let Some(d) = state::read_current_data() { data = d; }`（100-101 行）；128 行 notify 传 `&config.currency_symbol`。
5. **main.rs**：`Commands` 枚举 30-89 行（Mod/Theme/Widget 均有 `#[command(subcommand)]` 内嵌枚举）；dispatch match 181-241 行（`Commands::Mod(cmd) => handle_mod(...)` 模式）；`inject_help` 246-302 行用 `mut_subcommand` 逐层注入翻译；`handle_mod` 427 行起（println + tr 模式）；history 符号消费点 939、941、999、1034 行 `&config.currency_symbol`。
6. **cc_config.rs**：`merge_status_line`/`remove_status_line`/`has_status_line` + `parse_root`/`pretty` 私有助手（5-52 行）——env 块读写函数照此模式。
7. **serve.rs**：`build_api_json` 92-129 行（`let data = state::read_current_data().unwrap_or_default();` 97 行）；JS 硬编码 `'$'` 三处：297 行（val-cost）、303 行（val-week-cost）、328 行（trend bar title），均在 `refresh()` 内（289 行起），`data` 在作用域。
8. **doctor.rs**：`run` 11-124 行；`pricing_check` 227-259 行（`[pricing]` 负价 failure + 内置表信息项，本批次照此加 `model_check`）；doctor.rs 目前无 `#[cfg(test)]` 模块。
9. **state.rs**：ISO 时间戳既有模式 `chrono::DateTime::<chrono::Utc>::from_timestamp(now_secs() as i64, 0)...to_rfc3339()`（176-178 行）——chrono 无 clock feature，不能用 `Utc::now()`，synced_at 复用该模式。
10. **Cargo.toml**：ureq 2、tiny_http 0.12 已依赖（37-38 行），无 dev-dependencies。
11. **i18n**：en.toml/zh.toml 段结构 `[runtime]/[widget]/[notify]/[cli]/[web]`；zh_keys_are_subset_of_en_keys 测试强制键对等（本批次每任务必须双文件同步加键）。
12. **测试命令**：`export PATH="$HOME/.cargo/bin:$PATH" && cargo test <单一过滤词>`（多过滤参数会报 unexpected argument）。
13. **DeepSeek 数据**（spec §2 已确认）：flash 窗口 1M，USD $0.14/$0.28（cache 命中 $0.0028）、CNY ¥1/¥2（cache ¥0.02）；pro 窗口 1M，USD $0.435/$0.87（cache $0.003625，2026-05-24 后永久价）、CNY ¥3/¥6（推算值，发版前核对）。cache_creation 沿用 1.25×input 约定；CNY cache_read 沿用 0.02×input 比例。

---

## 任务 1：ModelEntry 数据模型 + config `[models]` 解析 + currency_symbol → Option + currency() 决议

**Files:**
- Modify: `src/core/pricing.rs`（ModelEntry 结构，紧邻 PriceEntry）
- Modify: `src/core/config.rs:43-44`（currency_symbol 改 Option）、`:50`（加 models 段）、`:57-59`（删 default_currency_symbol）、`:387`（Default 改 None）、尾部（currency() 方法 + 测试）
- Test: `src/core/config.rs` tests 模块、`src/core/pricing.rs` tests 模块（编译失败即 RED）

- [ ] **Step 1: 更新既有测试 + 写新失败测试（config.rs tests 模块）**

`src/core/config.rs` tests 中既有 `currency_symbol_and_pricing_defaults`（约 413-415 行）断言 `c.currency_symbol == "$"`，改为：

```rust
#[test]
fn currency_symbol_and_pricing_defaults() {
    let c = AppConfig::default();
    assert!(c.currency_symbol.is_none(), "default currency_symbol is None");
    assert_eq!(c.currency(), "$");
    assert!(c.pricing.is_empty());
}
```

同模块追加（RED：`models` 字段与 `currency()` 方法不存在 → 编译失败）：

```rust
#[test]
fn models_section_parsed_with_dual_currency() {
    let toml_str = r#"
        [models."deepseek-v4-flash"]
        context_window = 1000000
        synced_at = "2026-08-05T12:00:00Z"
        [models."deepseek-v4-flash".price_usd]
        input = 0.14e-6
        output = 0.28e-6
        cache_read = 0.0028e-6
        cache_creation = 0.175e-6
        [models."deepseek-v4-flash".price_cny]
        input = 1.0e-6
        output = 2.0e-6
        cache_read = 0.02e-6
        cache_creation = 1.25e-6
        [pricing."my-model"]
        input = 1.0e-6
        output = 2.0e-6
    "#;
    let cfg: AppConfig = toml::from_str(toml_str).unwrap();
    let entry = cfg.models.get("deepseek-v4-flash").expect("model entry parsed");
    assert_eq!(entry.context_window, Some(1_000_000));
    assert_eq!(entry.synced_at.as_deref(), Some("2026-08-05T12:00:00Z"));
    let usd = entry.price_usd.as_ref().expect("usd price");
    assert!((usd.input - 0.14e-6).abs() < 1e-15);
    let cny = entry.price_cny.as_ref().expect("cny price");
    assert!((cny.output - 2.0e-6).abs() < 1e-15);
    // [pricing] 与 [models] 并存
    assert!(cfg.pricing.contains_key("my-model"));
}

#[test]
fn currency_resolution_explicit_zh_default_en() {
    let mut zh = AppConfig::default();
    zh.language = "zh".into();
    assert_eq!(zh.currency(), "¥", "zh without explicit symbol");
    let mut explicit = AppConfig::default();
    explicit.currency_symbol = Some("€".into());
    assert_eq!(explicit.currency(), "€", "explicit wins");
    let mut zh_explicit = AppConfig::default();
    zh_explicit.language = "zh".into();
    zh_explicit.currency_symbol = Some("$".into());
    assert_eq!(zh_explicit.currency(), "$", "explicit beats zh default");
    assert_eq!(AppConfig::default().currency(), "$", "en default");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test currency_resolution_explicit_zh_default_en`
Expected: 编译失败 E0609（`models`/`currency` 不存在）。随后 `cargo test currency_symbol_and_pricing_defaults` 也应失败（String vs Option 类型不匹配）。

- [ ] **Step 3: 实现 ModelEntry + config 改造**

`src/core/pricing.rs` 的 `PriceEntry` 后追加：

```rust
/// 模型注册表条目：真实窗口 + 双币种价格（任一字段缺失即"无此覆盖"）。
/// 来源：内置表（二进制打包）/ GitHub sync（写 config [models] 带 synced_at）/ 用户手写。
#[derive(Debug, Clone, Default, Deserialize, Serialize)]
pub struct ModelEntry {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub context_window: Option<u64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub synced_at: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub price_usd: Option<PriceEntry>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub price_cny: Option<PriceEntry>,
}
```

`src/core/config.rs` 三处修改：

```rust
// 1) 43-44 行替换：
    #[serde(default)]
    pub currency_symbol: Option<String>,

// 2) 50 行 pricing 字段后追加：
    #[serde(default)]
    pub models: HashMap<String, crate::core::pricing::ModelEntry>,

// 3) 删除 default_currency_symbol()（57-59 行）
```

`src/core/config.rs` 手动 Default（387 行附近）`currency_symbol: "$".into(),` 改为 `currency_symbol: None,`。

`src/core/config.rs` 文件头 import 追加一行：`use crate::core::i18n::Language;`

类型变更连带修正：`src/core/pricing.rs` tests（约 310 行）`config.currency_symbol = "¥".into();` 改为 `config.currency_symbol = Some("¥".into());`（Option 类型编译修正，行为不变）。

`src/core/config.rs` 追加方法（放 `language()` 附近）：

```rust
/// 币种符号决议：显式配置 → zh 语言 ¥ → 其他 $。
pub fn currency(&self) -> &str {
    if let Some(s) = self.currency_symbol.as_deref() {
        return s;
    }
    match self.language() {
        Language::Zh => "¥",
        _ => "$",
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test currency_` + `cargo test models_section_parsed_with_dual_currency` + `cargo test currency_symbol_and_pricing_defaults`
Expected: 3 项全 PASS；随后 `cargo test` 全量确认无其他破坏（pricing.rs 310 行类型修正已在 Step 3 完成）。

- [ ] **Step 5: 批次末统一提交（用户授权后）**

---

## 任务 2：builtin_models() 内置注册表（9 Claude + 2 DeepSeek 双币种）

**Files:**
- Modify: `src/core/pricing.rs`（builtin_models()，紧邻 builtin_pricing()）
- Test: `src/core/pricing.rs` tests 模块

- [ ] **Step 1: 写失败测试（pricing.rs tests 模块追加）**

```rust
#[test]
fn builtin_models_cover_claude_and_deepseek() {
    let m = builtin_models();
    assert_eq!(m.len(), 11, "9 claude + 2 deepseek");
    for id in ["claude-opus-4-7", "claude-sonnet-4-6", "claude-haiku-4-5"] {
        let e = m.get(id).unwrap_or_else(|| panic!("{id} in builtin"));
        assert_eq!(e.context_window, Some(200_000), "{id} window 200k");
        assert!(e.price_usd.is_some(), "{id} usd price");
        assert!(e.price_cny.is_none(), "{id} no cny price");
    }
    let flash = m.get("deepseek-v4-flash").expect("flash in builtin");
    assert_eq!(flash.context_window, Some(1_000_000));
    let usd = flash.price_usd.as_ref().expect("flash usd");
    assert!((usd.input - 0.14e-6).abs() < 1e-15);
    assert!((usd.output - 0.28e-6).abs() < 1e-15);
    assert!((usd.cache_read - 0.0028e-6).abs() < 1e-15);
    let cny = flash.price_cny.as_ref().expect("flash cny");
    assert!((cny.input - 1.0e-6).abs() < 1e-15);
    assert!((cny.output - 2.0e-6).abs() < 1e-15);
    assert!((cny.cache_read - 0.02e-6).abs() < 1e-15);
    let pro = m.get("deepseek-v4-pro").expect("pro in builtin");
    assert_eq!(pro.context_window, Some(1_000_000));
    let usd_pro = pro.price_usd.as_ref().expect("pro usd");
    assert!((usd_pro.input - 0.435e-6).abs() < 1e-15);
    assert!((usd_pro.output - 0.87e-6).abs() < 1e-15);
    assert!((usd_pro.cache_read - 0.003625e-6).abs() < 1e-15);
    assert!((usd_pro.cache_creation - 0.54375e-6).abs() < 1e-15, "pro cache_creation = 1.25x input");
    assert!(pro.price_cny.is_some(), "pro cny present (derived, verify before release)");
}

#[test]
fn builtin_models_price_matches_builtin_pricing_for_claude() {
    let b = builtin_pricing();
    let m = builtin_models();
    for (id, price) in &b {
        let e = m.get(id).expect("same ids");
        let p = e.price_usd.as_ref().expect("usd");
        assert!((p.input - price.input).abs() < 1e-15, "{id} input");
        assert!((p.output - price.output).abs() < 1e-15, "{id} output");
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test builtin_models_cover_claude_and_deepseek`
Expected: FAIL `cannot find function builtin_models`。

- [ ] **Step 3: 实现 builtin_models()（pricing.rs，builtin_pricing() 之后）**

```rust
/// 内置模型注册表：9 个 Claude 模型（价格复用 builtin_pricing + 窗口 200k）
/// + DeepSeek 种子（窗口 1M、双币种真实发布价；v4-pro CNY 为峰值价推算，
/// 发版前以官方中文价目页核对）。随二进制发版刷新；用户 [pricing]/[models] 可覆盖。
pub fn builtin_models() -> HashMap<String, ModelEntry> {
    let mut out: HashMap<String, ModelEntry> = builtin_pricing()
        .into_iter()
        .map(|(id, price)| {
            let mut e = ModelEntry::default();
            e.context_window = Some(200_000);
            e.price_usd = Some(price);
            (id, e)
        })
        .collect();
    out.insert(
        "deepseek-v4-flash".into(),
        ModelEntry {
            context_window: Some(1_000_000),
            synced_at: None,
            price_usd: Some(PriceEntry {
                input: 0.14e-6,
                output: 0.28e-6,
                cache_read: 0.0028e-6,
                cache_creation: 0.175e-6,
            }),
            price_cny: Some(PriceEntry {
                input: 1.0e-6,
                output: 2.0e-6,
                cache_read: 0.02e-6,
                cache_creation: 1.25e-6,
            }),
        },
    );
    out.insert(
        "deepseek-v4-pro".into(),
        ModelEntry {
            context_window: Some(1_000_000),
            synced_at: None,
            price_usd: Some(PriceEntry {
                input: 0.435e-6,
                output: 0.87e-6,
                cache_read: 0.003625e-6,
                cache_creation: 0.54375e-6,
            }),
            price_cny: Some(PriceEntry {
                input: 3.0e-6,
                output: 6.0e-6,
                cache_read: 0.06e-6,
                cache_creation: 3.75e-6,
            }),
        },
    );
    out
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test builtin_models_cover_claude_and_deepseek` + `cargo test builtin_models_price_matches_builtin_pricing_for_claude`
Expected: 2 项全 PASS。

---

## 任务 3：resolve_context_window 窗口单点解析 + 三处管线接线

**Files:**
- Modify: `src/core/pricing.rs`（model_window + resolve_context_window + 测试）
- Modify: `src/compact.rs:99-108`（render()：data 改 mut + resolve）
- Modify: `src/dashboard.rs:79`、`:100-101`（run_loop 两处 data 更新后 resolve）
- Modify: `src/serve.rs:92-129`（build_api_json：data 改 mut + resolve）

- [ ] **Step 1: 写失败测试（pricing.rs tests 模块追加）**

```rust
fn window_session(win: u64, pct: f64, t_in: u64, t_out: u64) -> SessionData {
    SessionData::from_stdin_json(&format!(
        r#"{{"model":{{"id":"deepseek-v4-flash","display_name":"F"}},
            "context_window":{{"used_percentage":{pct},"total_input_tokens":{t_in},
                             "total_output_tokens":{t_out},"context_window_size":{win}}},
            "cost":{{"total_cost_usd":0.0,"total_duration_ms":0}}}}"#
    ))
    .unwrap()
}

fn cfg_with_models(entries: &[(&str, Option<u64>)]) -> AppConfig {
    let mut c = AppConfig::default();
    for (id, window) in entries {
        c.models.insert(
            id.to_string(),
            crate::core::pricing::ModelEntry { context_window: *window, ..Default::default() },
        );
    }
    c
}

#[test]
fn resolve_no_override_keeps_stdin() {
    let mut data = window_session(200_000, 44.0, 88_000, 857);
    let cfg = AppConfig::default();
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.context_window_size, 200_000);
    assert_eq!(data.context_window.used_percentage, 44.0, "trust stdin pct");
}

#[test]
fn resolve_different_window_recomputes_pct() {
    // stdin 200k pct=44；[models] 窗口 1M → (88000+857)/1M×100 = 8.8857
    let mut data = window_session(200_000, 44.0, 88_000, 857);
    let cfg = cfg_with_models(&[("deepseek-v4-flash", Some(1_000_000))]);
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.context_window_size, 1_000_000, "size overwritten");
    assert!((data.context_window.used_percentage - 8.8857).abs() < 0.001, "pct recomputed");
}

#[test]
fn resolve_same_window_trusts_stdin_pct() {
    let mut data = window_session(1_000_000, 9.0, 88_000, 857);
    let cfg = cfg_with_models(&[("deepseek-v4-flash", Some(1_000_000))]);
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.used_percentage, 9.0, "no recompute");
}

#[test]
fn resolve_clamps_pct_to_100() {
    let mut data = window_session(200_000, 44.0, 950_000, 900_000); // used > window
    let cfg = cfg_with_models(&[("deepseek-v4-flash", Some(1_000_000))]);
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.used_percentage, 100.0, "clamped");
}

#[test]
fn resolve_zero_window_skipped() {
    let mut data = window_session(200_000, 44.0, 88_000, 857);
    let cfg = cfg_with_models(&[("deepseek-v4-flash", Some(0))]);
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.context_window_size, 200_000, "0 treated as invalid");
    assert_eq!(data.context_window.used_percentage, 44.0);
}

#[test]
fn resolve_builtin_window_applies_without_config() {
    let mut data = window_session(200_000, 44.0, 88_000, 857);
    let cfg = AppConfig::default(); // 无 [models] → 内置表兜底
    resolve_context_window(&mut data, &cfg);
    assert_eq!(data.context_window.context_window_size, 1_000_000, "builtin 1M");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test resolve_`
Expected: FAIL `cannot find function resolve_context_window`。

- [ ] **Step 3: 实现（pricing.rs，merged_pricing() 之后）**

```rust
/// 有效窗口链：[models].context_window → 内置表 → None（stdin 原值）。
pub fn model_window(config: &AppConfig, model_id: &str) -> Option<u64> {
    if let Some(w) = config.models.get(model_id).and_then(|m| m.context_window) {
        return Some(w);
    }
    builtin_models().get(model_id).and_then(|m| m.context_window)
}

/// 窗口单点解析（管线入口调用，一次生效全线消费点：context_bar pct+ETA、
/// alerts 压缩告警、compact 压缩通知、serve pct、dashboard 卡片）。
/// 有效窗口 ≠ stdin 窗口 → 用 (t_in + t_out) 重算 pct（钳制 ≤100）并回写 size；
/// 相同 → 信任 stdin pct（不重算）；窗口 0 视为无效，跳过。
pub fn resolve_context_window(data: &mut SessionData, config: &AppConfig) {
    let Some(effective) = model_window(config, &data.model.id) else {
        return;
    };
    if effective == 0 || effective == data.context_window.context_window_size {
        return;
    }
    let used = data.context_window.total_input_tokens + data.context_window.total_output_tokens;
    let pct = (used as f64 / effective as f64) * 100.0;
    data.context_window.context_window_size = effective;
    data.context_window.used_percentage = pct.min(100.0);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test resolve_`
Expected: 6 项全 PASS。

- [ ] **Step 5: 三处管线接线（编译 + 全量回归）**

`src/compact.rs` render()（99-108 行）改为：

```rust
    let stdin_data = read_stdin()?;
    let mut data = SessionData::from_stdin_json(&stdin_data)
        .map_err(|e| format!("parse stdin JSON: {}", e))?;
    // v0.7 窗口单点解析：真实窗口覆盖 200k 兜底（重算 pct，一次生效全线消费点）
    crate::core::pricing::resolve_context_window(&mut data, config);
    run_pipeline(&data, registry, config, theme)
```

`src/dashboard.rs` 79 行后追加（初始恢复）：

```rust
    crate::core::pricing::resolve_context_window(&mut data, config);
```

`src/dashboard.rs` 100-101 行刷新点后追加：

```rust
        if let Some(d) = state::read_current_data() {
            data = d;
            // v0.7 窗口单点解析（刷新数据同样覆盖 200k 兜底）
            crate::core::pricing::resolve_context_window(&mut data, config);
        }
```

`src/serve.rs` build_api_json 97 行改为：

```rust
    let mut data = state::read_current_data().unwrap_or_default();
    crate::core::pricing::resolve_context_window(&mut data, config);
```

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test` — 全量回归通过（含既有 compact/dashboard/serve 编译）。

---

## 任务 4：price_for(lang) 语言选币种 + merged_pricing 改造 + currency() 注入点替换（12 处）

**Files:**
- Modify: `src/core/pricing.rs`（price_for + merged_pricing 重写 + inject 两处 + 旧测试类型修正 + 新测试）
- Modify: `src/compact.rs:148`、`:167`（`&config.currency_symbol` → `config.currency()`）
- Modify: `src/dashboard.rs:128`
- Modify: `src/main.rs:939`、`:941`、`:999`、`:1034`
- Test: `src/core/pricing.rs` tests 模块

- [ ] **Step 1: 修正旧测试 + 写失败测试（pricing.rs tests）**

既有测试（约 310 行）`config.currency_symbol = "¥".into();` 改为 `config.currency_symbol = Some("¥".into());`（Option 类型编译修正，行为不变）。

追加失败测试：

```rust
fn cfg_lang_models(lang: &str, entries: &[(&str, Option<f64>, Option<f64>)]) -> AppConfig {
    // (model_id, usd_input, cny_input)；None 表示无该币种
    let mut c = AppConfig::default();
    c.language = lang.to_string();
    for (id, usd, cny) in entries {
        let mut e = ModelEntry::default();
        e.price_usd = usd.map(|i| PriceEntry { input: i, ..Default::default() });
        e.price_cny = cny.map(|i| PriceEntry { input: i, ..Default::default() });
        c.models.insert(id.to_string(), e);
    }
    c
}

#[test]
fn price_for_zh_prefers_cny() {
    let cfg = cfg_lang_models("zh", &[("m1", Some(0.5e-6), Some(3.0e-6))]);
    let p = price_for(&cfg, "m1", Language::Zh).expect("cny price");
    assert!((p.input - 3.0e-6).abs() < 1e-15, "zh uses cny");
}

#[test]
fn price_for_zh_missing_cny_falls_back_usd() {
    let cfg = cfg_lang_models("zh", &[("m1", Some(0.5e-6), None)]);
    let p = price_for(&cfg, "m1", Language::Zh).expect("usd fallback");
    assert!((p.input - 0.5e-6).abs() < 1e-15);
}

#[test]
fn price_for_en_prefers_usd() {
    let cfg = cfg_lang_models("en", &[("m1", Some(0.5e-6), Some(3.0e-6))]);
    let p = price_for(&cfg, "m1", Language::En).expect("usd price");
    assert!((p.input - 0.5e-6).abs() < 1e-15);
}

#[test]
fn price_for_user_pricing_wins_over_models() {
    let mut cfg = cfg_lang_models("zh", &[("m1", Some(0.5e-6), Some(3.0e-6))]);
    cfg.pricing.insert("m1".into(), PriceEntry { input: 9.9e-6, ..Default::default() });
    let p = price_for(&cfg, "m1", Language::Zh).expect("user [pricing] wins");
    assert!((p.input - 9.9e-6).abs() < 1e-15);
}

#[test]
fn price_for_builtin_deepseek_by_language() {
    let cfg = AppConfig::default(); // 无任何配置 → 内置表
    let p_zh = price_for(&cfg, "deepseek-v4-flash", Language::Zh).expect("builtin cny");
    assert!((p_zh.input - 1.0e-6).abs() < 1e-15);
    let p_en = price_for(&cfg, "deepseek-v4-flash", Language::En).expect("builtin usd");
    assert!((p_en.input - 0.14e-6).abs() < 1e-15);
}

#[test]
fn price_for_unknown_model_none() {
    let cfg = AppConfig::default();
    assert!(price_for(&cfg, "unknown-model", Language::Zh).is_none());
}

#[test]
fn merged_pricing_zh_uses_cny() {
    let cfg = cfg_lang_models("zh", &[("m1", Some(0.5e-6), Some(3.0e-6))]);
    let merged = merged_pricing(&cfg);
    let p = merged.get("m1").expect("m1 in merged");
    assert!((p.input - 3.0e-6).abs() < 1e-15, "zh merged uses cny");
}

#[test]
fn inject_currency_follows_language() {
    let mut cfg = AppConfig::default();
    cfg.language = "zh".into(); // currency_symbol 保持 None
    let data = SessionData::from_stdin_json(
        r#"{"model":{"id":"m","display_name":"M"},
            "context_window":{"total_input_tokens":1,"total_output_tokens":1,
                             "context_window_size":200000},
            "cost":{"total_cost_usd":0.0,"total_duration_ms":0}}"#,
    )
    .unwrap();
    let mut wc = WidgetConfig::default();
    inject_cost_realtime(&data, &cfg, &mut wc);
    assert_eq!(wc.get_str("currency_symbol", ""), "¥", "zh injects ¥");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test price_for_` + `cargo test merged_pricing_zh_uses_cny` + `cargo test inject_currency_follows_language`
Expected: FAIL `cannot find function price_for`；`inject_currency_follows_language` 得到 `$` 而非 `¥`。

- [ ] **Step 3: 实现 price_for + merged_pricing 重写 + inject 两处**

`src/core/pricing.rs` 文件头加 `use crate::core::i18n::Language;`（与既有 use 并列）。

`merged_pricing`（57-63 行）整体替换为：

```rust
/// 语言感知合并表（① 查询唯一入口）：zh 用 cny（缺则 usd 兜底），en 用 usd。
/// 链：[pricing]（用户手写，最高优先）→ [models] → 内置表；无覆盖 → 透传。
pub fn merged_pricing(config: &AppConfig) -> PricingTable {
    let lang = config.language();
    let mut ids: Vec<&String> = Vec::new();
    for id in config.pricing.keys().chain(config.models.keys()) {
        if !ids.contains(&id) {
            ids.push(id);
        }
    }
    for id in builtin_models().keys() {
        if !ids.contains(&id) {
            ids.push(id);
        }
    }
    let mut table = PricingTable::new();
    for id in ids {
        if let Some(p) = price_for(config, id, lang) {
            table.insert(id.clone(), p.clone());
        }
    }
    table
}

/// 语言选币种：zh 优先 cny（缺则回退 usd 价 + ¥ 符号），en 优先 usd。
/// 链：[pricing]（USD 语义，最高优先）→ [models] 该币种 → 内置表该币种 → None。
pub fn price_for<'a>(
    config: &'a AppConfig,
    model_id: &str,
    lang: Language,
) -> Option<&'a PriceEntry> {
    if let Some(p) = config.pricing.get(model_id) {
        return Some(p);
    }
    let entry = config
        .models
        .get(model_id)
        .or_else(|| builtin_models().get(model_id));
    match lang {
        Language::Zh => entry
            .and_then(|m| m.price_cny.as_ref())
            .or_else(|| entry.and_then(|m| m.price_usd.as_ref())),
        Language::En => entry.and_then(|m| m.price_usd.as_ref()),
    }
}
```

`inject_cost`（164 行）与 `inject_cost_realtime`（189 行）的注入行改为：

```rust
    widget_config
        .values
        .insert("currency_symbol".into(), config.currency().to_string());
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test price_for_` + `cargo test merged_pricing_zh_uses_cny` + `cargo test inject_currency_follows_language` + `cargo test currency_`
Expected: 全 PASS。

- [ ] **Step 5: 其余 8 处消费点改走 currency()（编译 + 全量回归）**

```rust
// src/compact.rs:148 —— alert::send_notifications(...) 参数
        &config.currency(),

// src/compact.rs:167 —— crate::notify::budget(...) 参数
            config.currency(),

// src/dashboard.rs:128 —— notify 参数（同一模式）
            config.currency(),

// src/main.rs:939、941、999、1034 —— history 报告 symbol
    let symbol = config.currency();   // 939 行 return print_weekly_report(&store, config.currency(), lang)
    // 941/999/1034 行 `let symbol = &config.currency_symbol;` → `let symbol = config.currency();`
```

注意：939 行是 `print_weekly_report(&store, &config.currency_symbol, lang)` 的直接参数，改为 `config.currency()`；941/999/1034 行是局部 `let symbol = &config.currency_symbol;`，改为 `let symbol = config.currency();`（&str，后续 `format!("{}", symbol)` 等用法不变）。

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test` — 全量回归通过。

- [ ] **Step 6（2026-08-05 控制器修订，用户拍板）：预算告警统一语言选币种口径**

背景：语言选币种后，zh 用户成本走 ¥。用户决定：**预算告警（cap、warn_pcts、百分比）整体按当前语言选币种口径** —— 成本 ¥、上限即显示币种数值（zh 用户写 10 = 10 元）、百分比 ¥/上限。不做任何 USD 换算（延续 §11 不做汇率换算）；`cap_usd` 字段名与 `budget_cap_usd` widget 注入键保留（配置/API 兼容），仅语义与文档更新。

改动：
1. `src/compact.rs`（~144-171 行）：预算路径**逻辑不变**（Task 4 后 `effective_cost` 已走 `merged_pricing` = 语言币种）；注释明确口径："预算上限为显示币种数值（语言选币种，zh 即元）；成本 ¥、百分比 ¥/上限，无汇率换算"。
2. `src/dashboard.rs`（~127-131 行）：`effective_cost` 参数从 `&config.pricing`（绕过合并表，与 compact 不一致 → 告警正文口径分裂）改为 `&pricing::merged_pricing(config)`。
3. 文档注释：`src/core/config.rs` [budget] 段与 `BudgetConfig.cap_usd` 字段 doc、`src/alert.rs` `check_budget` doc —— 明确"cap 为显示币种数值（语言选币种）；zh 用户即人民币，en 即美元"。
4. 测试：pricing.rs 加 `budget_cost_is_language_priced`（zh 配置 + deepseek + 100k input → realtime_cost(merged_pricing) = 0.1 = ¥1/M×0.1M，钉住预算成本为语言币种）；全量回归。

---

## 任务 5：cc_config env 块读写（CLAUDE_CODE_MAX_CONTEXT_TOKENS）

**Files:**
- Modify: `src/core/cc_config.rs`（set_env_window / remove_env_window / get_env_window + 测试）

- [ ] **Step 1: 写失败测试（cc_config.rs tests 模块追加）**

```rust
#[test]
fn set_env_window_creates_block() {
    let out = set_env_window("{}", 1_000_000).unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert_eq!(root["env"]["CLAUDE_CODE_MAX_CONTEXT_TOKENS"], "1000000");
}

#[test]
fn set_env_window_preserves_other_keys_and_env() {
    let out = set_env_window(
        r#"{"apiKeyHelper":{"alwaysAllowedTools":[]},"env":{"ANTHROPIC_BASE_URL":"https://x"}}"#,
        1_000_000,
    )
    .unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert!(root.get("apiKeyHelper").is_some(), "other top-level keys kept");
    assert_eq!(root["env"]["ANTHROPIC_BASE_URL"], "https://x", "other env kept");
    assert_eq!(root["env"]["CLAUDE_CODE_MAX_CONTEXT_TOKENS"], "1000000");
}

#[test]
fn set_env_window_replaces_existing_value() {
    let out = set_env_window(r#"{"env":{"CLAUDE_CODE_MAX_CONTEXT_TOKENS":"200000"}}"#, 500_000).unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert_eq!(root["env"]["CLAUDE_CODE_MAX_CONTEXT_TOKENS"], "500000");
    assert_eq!(root["env"].as_object().unwrap().len(), 1, "no duplication");
}

#[test]
fn set_env_window_invalid_inputs_err() {
    assert!(set_env_window("{not json", 1).is_err());
    assert!(set_env_window("[1,2]", 1).is_err(), "non-object root");
    assert!(set_env_window(r#"{"env": 42}"#, 1).is_err(), "non-object env");
}

#[test]
fn remove_env_window_removes_key_keeps_env() {
    let out = remove_env_window(r#"{"env":{"CLAUDE_CODE_MAX_CONTEXT_TOKENS":"1000000","X":"y"}}"#).unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert!(root["env"].get("CLAUDE_CODE_MAX_CONTEXT_TOKENS").is_none());
    assert_eq!(root["env"]["X"], "y", "other env kept");
}

#[test]
fn remove_env_window_drops_empty_env_block() {
    let out = remove_env_window(r#"{"env":{"CLAUDE_CODE_MAX_CONTEXT_TOKENS":"1000000"}}"#).unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert!(root.get("env").is_none(), "empty env removed");
}

#[test]
fn remove_env_window_missing_is_noop() {
    let out = remove_env_window(r#"{"permissions":{}}"#).unwrap();
    let root: Value = serde_json::from_str(&out).unwrap();
    assert!(root.get("permissions").is_some());
    assert!(root.get("env").is_none());
}

#[test]
fn get_env_window_reads_value() {
    assert_eq!(get_env_window(r#"{"env":{"CLAUDE_CODE_MAX_CONTEXT_TOKENS":"1000000"}}"#),
               Some("1000000".to_string()));
    assert_eq!(get_env_window(r#"{"permissions":{}}"#), None);
    assert_eq!(get_env_window(""), None);
    assert_eq!(get_env_window("{bad"), None, "unparseable → None");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test env_window`
Expected: FAIL `cannot find function set_env_window`（编译失败）。

- [ ] **Step 3: 实现（cc_config.rs，has_status_line() 之后）**

```rust
pub const ENV_WINDOW_KEY: &str = "CLAUDE_CODE_MAX_CONTEXT_TOKENS";

/// 在 settings.json 的 env 块写入 CLAUDE_CODE_MAX_CONTEXT_TOKENS（字符串值）。
/// env 不存在则创建；其他 env 键与顶层键全部保留。
pub fn set_env_window(existing: &str, window: u64) -> Result<String, String> {
    let mut root = parse_root(existing)?;
    if root.is_null() {
        root = Value::Object(Map::new());
    }
    if !root.is_object() {
        return Err("settings.json must be a JSON object at top level".to_string());
    }
    let env = match root.get("env") {
        None => Value::Object(Map::new()),
        Some(v) if v.is_object() => v.clone(),
        Some(_) => return Err("settings.json env must be a JSON object".to_string()),
    };
    if let Some(obj) = env.as_object_mut() {
        obj.insert(ENV_WINDOW_KEY.to_string(), Value::String(window.to_string()));
    }
    root["env"] = env;
    pretty(&root)
}

/// 从 settings.json env 块移除 CLAUDE_CODE_MAX_CONTEXT_TOKENS；env 变空则整块删除。
pub fn remove_env_window(existing: &str) -> Result<String, String> {
    let mut root = parse_root(existing)?;
    if root.is_null() {
        root = Value::Object(Map::new());
    }
    if let Some(env) = root.get_mut("env") {
        if let Some(obj) = env.as_object_mut() {
            obj.remove(ENV_WINDOW_KEY);
        }
        if env.as_object().map(|o| o.is_empty()).unwrap_or(false) {
            if let Some(obj) = root.as_object_mut() {
                obj.remove("env");
            }
        }
    }
    pretty(&root)
}

/// 读取 env 块中的窗口值；缺失/解析失败 → None。
pub fn get_env_window(existing: &str) -> Option<String> {
    let root = parse_root(existing).ok()?;
    root.get("env")?.get(ENV_WINDOW_KEY)?.as_str().map(String::from)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test env_window`
Expected: 8 项全 PASS。

---

## 任务 6：modelsync.rs 核心（拉取 / 解析校验 / 合并写 config）

**Files:**
- Create: `src/core/modelsync.rs`（registry_url / fetch_registry / parse_registry / merge_into_config / settings_path / now_iso + 测试）
- Modify: `src/core/mod.rs`（`pub mod modelsync;` 注册）
- Create: `registry/models.toml`（先放最小占位，任务 10 补全）——本任务只需测试 fixture，仓库文件在任务 10 建。

- [ ] **Step 1: 建文件 + 注册模块 + 写失败测试（RED）**

`src/core/mod.rs` 检查现有 `pub mod ...` 列表（含 pricing），追加一行 `pub mod modelsync;`。

`src/core/modelsync.rs` 只写测试骨架（引用待实现函数 → 编译失败即 RED）：

```rust
use std::collections::HashMap;
use std::path::PathBuf;

use serde::{Deserialize, Serialize};

use super::config::AppConfig;
use super::i18n::{tr, Language};
use super::pricing::{ModelEntry, PriceEntry};

pub const REGISTRY_URL: &str =
    "https://raw.githubusercontent.com/cuishiying-1/claude-hud/master/registry/models.toml";

/// 测试钩子：HUD_REGISTRY_URL 环境变量覆盖拉取地址。
pub fn registry_url() -> String {
    std::env::var("HUD_REGISTRY_URL").unwrap_or_else(|_| REGISTRY_URL.to_string())
}

/// 注册表文件（registry/models.toml）：registry_version + [models] 段。
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct RegistryFile {
    pub registry_version: String,
    #[serde(default)]
    pub models: HashMap<String, ModelEntry>,
}

#[cfg(test)]
mod tests {
    use super::*;

    const FIXTURE: &str = r#"
registry_version = "2026-08-05"
[models."deepseek-v4-flash"]
context_window = 1000000
[models."deepseek-v4-flash".price_usd]
input = 0.14e-6
output = 0.28e-6
cache_read = 0.0028e-6
cache_creation = 0.175e-6
[models."deepseek-v4-flash".price_cny]
input = 1.0e-6
output = 2.0e-6
cache_read = 0.02e-6
cache_creation = 1.25e-6
"#;

    #[test]
    fn parse_valid_dual_currency_fixture() {
        let reg = parse_registry(FIXTURE).unwrap();
        assert_eq!(reg.registry_version, "2026-08-05");
        let e = reg.models.get("deepseek-v4-flash").unwrap();
        assert_eq!(e.context_window, Some(1_000_000));
        assert!(e.price_usd.is_some() && e.price_cny.is_some());
    }

    #[test]
    fn parse_missing_optional_fields_ok() {
        let reg = parse_registry("registry_version = \"2026-08-05\"\n[models.\"m\"]\ncontext_window = 8000\n").unwrap();
        let e = reg.models.get("m").unwrap();
        assert_eq!(e.context_window, Some(8000));
        assert!(e.price_usd.is_none() && e.price_cny.is_none());
    }

    #[test]
    fn parse_zero_window_rejected() {
        let err = parse_registry("registry_version = \"2026-08-05\"\n[models.\"m\"]\ncontext_window = 0\n").unwrap_err();
        assert!(err.contains("context_window must be > 0"), "err: {}", err);
    }

    #[test]
    fn parse_negative_price_rejected() {
        let err = parse_registry("registry_version = \"2026-08-05\"\n[models.\"m\"]\n[models.\"m\".price_cny]\ninput = -1.0e-6\n").unwrap_err();
        assert!(err.contains("negative price"), "err: {}", err);
    }

    #[test]
    fn parse_invalid_toml_rejected() {
        assert!(parse_registry("registry_version = [").is_err());
    }

    #[test]
    fn parse_missing_version_rejected() {
        assert!(parse_registry("[models.\"m\"]\n").unwrap_err().contains("registry_version"));
    }

    fn temp_dir(tag: &str) -> PathBuf {
        let d = std::env::temp_dir().join(format!("hud-ms-{}-{}", tag, std::process::id()));
        let _ = std::fs::create_dir_all(&d);
        d
    }

    #[test]
    fn merge_preserves_user_entries_and_other_sections() {
        let dir = temp_dir("merge1");
        let cfg_path = dir.join("config.toml");
        std::fs::write(
            &cfg_path,
            "[pricing.\"my-model\"]\ninput = 1.0e-6\noutput = 2.0e-6\n[models.\"user-hand\"].context_window = 1000\n",
        )
        .unwrap();
        let reg = parse_registry(FIXTURE).unwrap();
        let updated = merge_into_config(&cfg_path, &reg, "2026-08-05T00:00:00Z").unwrap();
        assert_eq!(updated, vec!["deepseek-v4-flash".to_string()]);
        let re: toml::Value = toml::from_str(&std::fs::read_to_string(&cfg_path).unwrap()).unwrap();
        assert_eq!(re["models"]["deepseek-v4-flash"]["context_window"], toml::Value::Integer(1_000_000));
        assert_eq!(re["models"]["deepseek-v4-flash"]["synced_at"], toml::Value::String("2026-08-05T00:00:00Z".into()));
        assert_eq!(re["models"]["user-hand"]["context_window"], toml::Value::Integer(1000), "user entry kept");
        assert_eq!(re["pricing"]["my-model"]["input"], toml::Value::Float(1.0e-6), "pricing section kept");
    }

    #[test]
    fn merge_overwrites_existing_registry_entry() {
        let dir = temp_dir("merge2");
        let cfg_path = dir.join("config.toml");
        std::fs::write(
            &cfg_path,
            "[models.\"deepseek-v4-flash\"].context_window = 200000\n",
        )
        .unwrap();
        let reg = parse_registry(FIXTURE).unwrap();
        merge_into_config(&cfg_path, &reg, "2026-08-05T00:00:00Z").unwrap();
        let re: toml::Value = toml::from_str(&std::fs::read_to_string(&cfg_path).unwrap()).unwrap();
        assert_eq!(re["models"]["deepseek-v4-flash"]["context_window"], toml::Value::Integer(1_000_000), "overwritten");
    }

    #[test]
    fn fetch_failure_returns_err() {
        assert!(fetch_registry("http://127.0.0.1:1/registry.toml").is_err());
    }

    #[test]
    fn registry_url_respects_env_hook() {
        std::env::set_var("HUD_REGISTRY_URL", "http://127.0.0.1:9999/x");
        assert_eq!(registry_url(), "http://127.0.0.1:9999/x");
        std::env::remove_var("HUD_REGISTRY_URL");
        assert_eq!(registry_url(), REGISTRY_URL);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test modelsync`
Expected: 编译失败 `cannot find function parse_registry`（fetch_registry / merge_into_config 等同理）。

- [ ] **Step 3: 实现（modelsync.rs，tests 模块前）**

```rust
use std::io::Read;
use std::path::Path;

/// 拉取注册表（ureq，10s 超时，UA 与 update.rs 一致）。失败带原因返回。
pub fn fetch_registry(url: &str) -> Result<String, String> {
    let mut resp = ureq::get(url)
        .timeout(std::time::Duration::from_secs(10))
        .set("User-Agent", "claude-hud")
        .call()
        .map_err(|e| format!("fetch registry: {}", e))?;
    let mut body = String::new();
    resp.body_mut()
        .read_to_string(&mut body)
        .map_err(|e| format!("read registry response: {}", e))?;
    Ok(body)
}

/// 解析校验注册表：TOML 合法、registry_version 非空、窗口 >0、价格 ≥0。
/// 失败带模型定位错误，调用方不写任何文件。
pub fn parse_registry(content: &str) -> Result<RegistryFile, String> {
    let reg: RegistryFile =
        toml::from_str(content).map_err(|e| format!("parse registry: {}", e))?;
    if reg.registry_version.trim().is_empty() {
        return Err("registry_version missing".to_string());
    }
    for (id, entry) in &reg.models {
        if entry.context_window == Some(0) {
            return Err(format!("model {}: context_window must be > 0", id));
        }
        for (cur, p) in [("usd", &entry.price_usd), ("cny", &entry.price_cny)] {
            if let Some(p) = p {
                if p.input < 0.0 || p.output < 0.0 || p.cache_read < 0.0 || p.cache_creation < 0.0 {
                    return Err(format!("model {}: negative price in {}", id, cur));
                }
            }
        }
    }
    Ok(reg)
}

/// 把注册表条目合并进 config.toml 的 [models] 段（toml::Value 手术，保留
/// 其他配置段与用户手写条目）：远程覆盖/补齐本地，写 synced_at 时间戳；
/// 不删除本地任何条目。返回被更新（含新增）的模型 id。
pub fn merge_into_config(
    config_path: &Path,
    reg: &RegistryFile,
    synced_at: &str,
) -> Result<Vec<String>, String> {
    let existing = std::fs::read_to_string(config_path).unwrap_or_default();
    let mut root: toml::Value = if existing.trim().is_empty() {
        toml::Value::Table(toml::Table::new())
    } else {
        toml::from_str(&existing).map_err(|e| format!("parse config.toml: {}", e))?
    };
    let models = root
        .as_table_mut()
        .ok_or("config.toml must be a table")?
        .entry("models")
        .or_insert_with(|| toml::Value::Table(toml::Table::new()));
    let table = models
        .as_table_mut()
        .ok_or("config.toml [models] must be a table")?;
    let mut updated = Vec::new();
    for (id, entry) in &reg.models {
        let mut e = entry.clone();
        e.synced_at = Some(synced_at.to_string());
        let v = toml::Value::try_from(&e).map_err(|err| format!("serialize model {}: {}", id, err))?;
        table.insert(id.clone(), v);
        updated.push(id.clone());
    }
    let out = toml::to_string_pretty(&root).map_err(|e| format!("serialize config: {}", e))?;
    std::fs::write(config_path, out).map_err(|e| format!("write config: {}", e))?;
    Ok(updated)
}

/// ~/.claude/settings.json（与 doctor.rs status_line_ok 同路径解析）。
pub fn settings_path() -> Result<PathBuf, String> {
    let home = dirs::home_dir().ok_or("cannot find home directory")?;
    Ok(home.join(".claude").join("settings.json"))
}

/// ISO 时间戳（chrono 无 clock feature，复用 state.rs 模式）。
pub fn now_iso() -> String {
    chrono::DateTime::<chrono::Utc>::from_timestamp(
        super::state::now_secs() as i64,
        0,
    )
    .map(|d| d.to_rfc3339())
    .unwrap_or_default()
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test modelsync`
Expected: 9 项全 PASS。

- [ ] **Step 5: 全量回归**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test`
Expected: 全 PASS（`registry_url_respects_env_hook` 用了 set_var，仅读取 registry_url() 一条路径，无并行冲突）。

---

## 任务 7：clap `model` 命令组 + sync/env/list 全实现 + i18n

**Files:**
- Modify: `src/main.rs`（ModelCommands 枚举 + Commands::Model + dispatch + inject_help + handle_model）
- Modify: `src/core/modelsync.rs`（run_sync / SyncResult / SyncPaths / model_env_cmd / model_list_cmd + 测试）
- Modify: `locales/en.toml`、`locales/zh.toml`（[cli] + [runtime] 键）

- [ ] **Step 1: 写失败测试（modelsync.rs tests 追加）**

e2e 测试用 `run_sync_at(url, ...)` 显式传本地 tiny_http 地址（不碰 HUD_REGISTRY_URL 环境变量，避免并行测试互踩）：

```rust
    fn local_server() -> String {
        // tiny_http 本地服务模拟 GitHub 注册表，返回 "http://127.0.0.1:PORT"
        let server = tiny_http::Server::http("127.0.0.1:0").unwrap();
        let addr = server.server_addr().to_string();
        let fixture = FIXTURE.to_string();
        std::thread::spawn(move || {
            if let Ok(req) = server.recv() {
                let _ = req.respond(tiny_http::Response::from_string(fixture));
            }
        });
        format!("http://{}/registry.toml", addr)
    }

    #[test]
    fn sync_prompt_no_writes_config_only() {
        let url = local_server();
        let dir = temp_dir("e2e1");
        let config_path = dir.join("config.toml");
        std::fs::write(&config_path, "[pricing.\"x\"]\ninput = 1.0e-6\n").unwrap();
        let settings_path = dir.join("settings.json");
        let paths = SyncPaths { config: config_path.clone(), settings: settings_path.clone() };
        let result = run_sync_at(&url, &paths, Language::En, &mut |_| false).unwrap();
        assert_eq!(result.version, "2026-08-05");
        assert_eq!(result.updated, vec!["deepseek-v4-flash".to_string()]);
        assert!(!result.env_written);
        assert!(result.env_failed.is_none());
        assert!(!settings_path.exists(), "env prompt declined → settings untouched");
        let re: toml::Value = toml::from_str(&std::fs::read_to_string(&config_path).unwrap()).unwrap();
        assert!(re["models"]["deepseek-v4-flash"]["synced_at"].is_str(), "synced_at written");
    }

    #[test]
    fn sync_prompt_yes_writes_env() {
        let url = local_server();
        let dir = temp_dir("e2e2");
        let paths = SyncPaths {
            config: dir.join("config.toml"),
            settings: dir.join("settings.json"),
        };
        let result = run_sync_at(&url, &paths, Language::En, &mut |_| true).unwrap();
        assert!(result.env_written);
        let content = std::fs::read_to_string(&paths.settings).unwrap();
        assert!(content.contains("CLAUDE_CODE_MAX_CONTEXT_TOKENS"));
        assert!(content.contains("1000000"));
    }

    #[test]
    fn sync_fetch_failure_returns_err_and_writes_nothing() {
        let dir = temp_dir("e2e3");
        let config_path = dir.join("config.toml");
        std::fs::write(&config_path, "[pricing.\"x\"]\ninput = 1.0e-6\n").unwrap();
        let paths = SyncPaths { config: config_path.clone(), settings: dir.join("settings.json") };
        let err = run_sync_at("http://127.0.0.1:1/registry.toml", &paths, Language::En, &mut |_| false).unwrap_err();
        assert!(err.contains("fetch"), "err: {}", err);
        assert_eq!(
            std::fs::read_to_string(&config_path).unwrap(),
            "[pricing.\"x\"]\ninput = 1.0e-6\n",
            "config untouched on failure"
        );
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test sync_prompt`
Expected: FAIL `cannot find` / 行为失败（run_sync_at/SyncPaths 未定义）。

- [ ] **Step 3: 实现 run_sync_at / run_sync / SyncPaths / SyncResult（modelsync.rs）**

```rust
/// sync 路径注入（测试用临时目录，CLI 用真实路径）。
#[derive(Debug, Clone)]
pub struct SyncPaths {
    pub config: PathBuf,
    pub settings: PathBuf,
}

/// sync 结果（CLI 打印用，测试断言用）。
#[derive(Debug)]
pub struct SyncResult {
    pub version: String,
    pub updated: Vec<String>,
    pub env_written: bool,
    pub env_failed: Option<String>,
}

/// `model sync` 入口（CLI 用）：URL 来自 HUD_REGISTRY_URL 钩子或默认地址。
pub fn run_sync(
    paths: &SyncPaths,
    lang: Language,
    prompt: &mut dyn FnMut(&str) -> bool,
) -> Result<SyncResult, String> {
    run_sync_at(&registry_url(), paths, lang, prompt)
}

/// `model sync` 主流程（URL 显式传入，测试用本地 tiny_http 地址）：
/// 拉取 → 解析校验 → 合并写 config.toml → 逐模型询问 env（prompt 可注入，
/// 测试传 |_| false 跳过）。任何失败不写任何文件。
fn run_sync_at(
    url: &str,
    paths: &SyncPaths,
    lang: Language,
    prompt: &mut dyn FnMut(&str) -> bool,
) -> Result<SyncResult, String> {
    let body = fetch_registry(url)?;
    let reg = parse_registry(&body)?;
    let synced_at = now_iso();
    let updated = merge_into_config(&paths.config, &reg, &synced_at)?;
    let mut env_written = false;
    let mut env_failed: Option<String> = None;
    for id in &updated {
        let Some(window) = reg.models.get(id).and_then(|m| m.context_window) else {
            continue;
        };
        let ask = tr(lang, "runtime.model_sync_env_prompt")
            .replace("{id}", id)
            .replace("{window}", &window.to_string());
        if !prompt(&ask) {
            continue;
        }
        let content = std::fs::read_to_string(&paths.settings).unwrap_or_default();
        let merged = crate::core::cc_config::set_env_window(&content, window)
            .map_err(|e| format!("prepare env: {}", e))?;
        match crate::core::state::write_atomic(&paths.settings, &merged) {
            Ok(()) => env_written = true,
            Err(e) => env_failed = Some(e),
        }
    }
    Ok(SyncResult { version: reg.registry_version, updated, env_written, env_failed })
}
```

> 偏离说明（有意的）：spec §5 示例询问"当前模型窗口"，但 sync 命令运行期不知道当前
> 模型（只有 render 从 stdin 知道）。实现为对**每个更新到且带窗口的模型**逐条询问，
> 用户对当前使用的模型答 y 即可，其余答 N。

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test sync_prompt` + `cargo test sync_fetch_failure_returns_err_and_writes_nothing`
Expected: 3 项全 PASS。

- [ ] **Step 5: main.rs clap 接线**

`src/main.rs` WidgetCommands 枚举后追加：

```rust
#[derive(Subcommand)]
enum ModelCommands {
    /// Sync model registry (windows & prices) from GitHub
    Sync,
    /// View/set/clear CLAUDE_CODE_MAX_CONTEXT_TOKENS in settings.json env
    Env {
        /// Window to set (omit to view, "off" to remove)
        arg: Option<String>,
    },
    /// List merged model registry (builtin + config, with source)
    List,
}
```

`Commands` 枚举 `Widget(WidgetCommands)` 后追加 `/// Model registry management` + `#[command(subcommand)]` + `Model(ModelCommands),`。

dispatch match（`Commands::Widget(cmd) => ...` 后）追加：

```rust
        Commands::Model(cmd) => handle_model(cmd, &config, lang),
```

`inject_help` 中 `mut_subcommand("widget", ...)` 块后追加：

```rust
        .mut_subcommand("model", |c| {
            c.about(tr(lang, "cli.model"))
                .mut_subcommand("sync", |s| s.about(tr(lang, "cli.model_sync")))
                .mut_subcommand("env", |s| s.about(tr(lang, "cli.model_env")))
                .mut_subcommand("list", |s| s.about(tr(lang, "cli.model_list")))
        })
```

`handle_widget` 后追加：

```rust
fn handle_model(
    cmd: ModelCommands,
    config: &AppConfig,
    lang: crate::core::i18n::Language,
) -> Result<(), String> {
    match cmd {
        ModelCommands::Sync => {
            let paths = crate::core::modelsync::SyncPaths {
                config: AppConfig::config_path()?,
                settings: crate::core::modelsync::settings_path()?,
            };
            let result = crate::core::modelsync::run_sync(&paths, lang, &mut prompt_yn)?;
            println!(
                "{}",
                tr(lang, "runtime.model_sync_ok")
                    .replace("{version}", &result.version)
                    .replace("{n}", &result.updated.len().to_string())
            );
            for id in &result.updated {
                println!("  - {}", id);
            }
            if let Some(e) = result.env_failed {
                println!("{}", tr(lang, "runtime.model_sync_env_fail").replace("{e}", &e));
            }
            Ok(())
        }
        ModelCommands::Env { arg } => model_env_cmd(config, arg.as_deref(), lang),
        ModelCommands::List => model_list_cmd(config, lang),
    }
}

/// 交互询问 [y/N]，默认 N（不写 env）。仅 Sync 命令使用。
fn prompt_yn(prompt: &str) -> bool {
    println!("{} [y/N]", prompt);
    let mut line = String::new();
    std::io::stdin()
        .read_line(&mut line)
        .map(|n| n > 0 && line.trim().eq_ignore_ascii_case("y"))
        .unwrap_or(false)
}
```

- [ ] **Step 6: 实现 model_env_cmd / model_list_cmd（modelsync.rs）**

```rust
/// `model env [<window>|off]`：查看 / 设置 / 清除 settings.json env。
pub fn model_env_cmd(
    config: &AppConfig,
    arg: Option<&str>,
    lang: Language,
) -> Result<(), String> {
    let settings = settings_path()?;
    let content = std::fs::read_to_string(&settings).unwrap_or_default();
    match arg {
        None => match crate::core::cc_config::get_env_window(&content) {
            Some(v) => println!("{}", tr(lang, "runtime.model_env_view").replace("{window}", &v)),
            None => println!("{}", tr(lang, "runtime.model_env_none")),
        },
        Some("off") => {
            let updated = crate::core::cc_config::remove_env_window(&content)?;
            crate::core::state::write_atomic(&settings, &updated)?;
            println!("{}", tr(lang, "runtime.model_env_off"));
        }
        Some(w) => {
            let window: u64 = w
                .parse()
                .map_err(|_| tr(lang, "runtime.model_env_bad").to_string())?;
            let updated = crate::core::cc_config::set_env_window(&content, window)?;
            crate::core::state::write_atomic(&settings, &updated)?;
            println!("{}", tr(lang, "runtime.model_env_set").replace("{window}", w));
        }
    }
    Ok(())
}

/// `model list`：合并视图（内置 + config，config 覆盖），标注来源/窗口/双币种。
pub fn model_list_cmd(config: &AppConfig, lang: Language) -> Result<(), String> {
    let mut ids: Vec<String> = Vec::new();
    for id in config.models.keys().chain(super::pricing::builtin_models().keys()) {
        if !ids.contains(id) {
            ids.push(id.clone());
        }
    }
    ids.sort();
    for id in ids {
        let cfg = config.models.get(&id);
        let builtin = super::pricing::builtin_models().get(&id);
        let source = match cfg {
            Some(_) => {
                if cfg.and_then(|m| m.synced_at.clone()).is_some() {
                    tr(lang, "runtime.model_src_synced")
                } else {
                    tr(lang, "runtime.model_src_user")
                }
            }
            None => tr(lang, "runtime.model_src_builtin"),
        };
        let window = cfg
            .and_then(|m| m.context_window)
            .or_else(|| builtin.and_then(|m| m.context_window));
        let usd = cfg
            .and_then(|m| m.price_usd.as_ref())
            .or_else(|| builtin.and_then(|m| m.price_usd.as_ref()))
            .map(fmt_price)
            .unwrap_or_else(|| "—".to_string());
        let cny = cfg
            .and_then(|m| m.price_cny.as_ref())
            .or_else(|| builtin.and_then(|m| m.price_cny.as_ref()))
            .map(fmt_price)
            .unwrap_or_else(|| "—".to_string());
        let synced = cfg
            .and_then(|m| m.synced_at.clone())
            .unwrap_or_default();
        println!(
            "{}",
            tr(lang, "runtime.model_list_line")
                .replace("{id}", &id)
                .replace("{source}", source)
                .replace(
                    "{window}",
                    &window.map(|w| w.to_string()).unwrap_or_else(|| "—".to_string())
                )
                .replace("{usd}", &usd)
                .replace("{cny}", &cny)
                .replace("{synced}", &synced)
        );
    }
    Ok(())
}

fn fmt_price(p: &PriceEntry) -> String {
    format!("{:.2}/{:.2}", p.input * 1e6, p.output * 1e6)
}
```

- [ ] **Step 7: i18n 键（en.toml + zh.toml 同步加，zh_keys_are_subset_of_en_keys 强制对等）**

`locales/en.toml` `[cli]` 段追加：

```toml
model = "Model registry management"
model_sync = "Sync model registry (windows & prices) from GitHub"
model_env = "View/set/clear CLAUDE_CODE_MAX_CONTEXT_TOKENS in settings.json env"
model_list = "List merged model registry (builtin + config, with source)"
```

`locales/zh.toml` `[cli]` 段追加：

```toml
model = "模型注册表管理"
model_sync = "从 GitHub 同步模型注册表（真实窗口与价格）"
model_env = "查看/设置/清除 settings.json env 中的 CLAUDE_CODE_MAX_CONTEXT_TOKENS"
model_list = "列出合并后的模型注册表（内置 + 配置，含来源）"
```

`locales/en.toml` `[runtime]` 段追加：

```toml
model_sync_env_prompt = "Model {id} window is {window}. Write CLAUDE_CODE_MAX_CONTEXT_TOKENS to settings.json env?"
model_sync_ok = "Registry v{version} synced: {n} models updated"
model_sync_env_fail = "Registry synced, but env write failed: {e}"
model_env_view = "CLAUDE_CODE_MAX_CONTEXT_TOKENS = {window}"
model_env_none = "CLAUDE_CODE_MAX_CONTEXT_TOKENS is not set"
model_env_set = "env set: CLAUDE_CODE_MAX_CONTEXT_TOKENS={window} (restart Claude Code to apply; may not work on third-party backends)"
model_env_off = "env entry removed"
model_env_bad = "window must be a positive integer or \"off\""
model_list_line = "{id}  [{source}]  window={window}  usd={usd}  cny={cny}  synced={synced}"
model_src_synced = "synced"
model_src_user = "user"
model_src_builtin = "builtin"
```

`locales/zh.toml` `[runtime]` 段追加：

```toml
model_sync_env_prompt = "模型 {id} 窗口为 {window}。是否写入 settings.json env（CLAUDE_CODE_MAX_CONTEXT_TOKENS）？"
model_sync_ok = "注册表 v{version} 已同步：更新 {n} 个模型"
model_sync_env_fail = "注册表已同步，但 env 写入失败：{e}"
model_env_view = "CLAUDE_CODE_MAX_CONTEXT_TOKENS = {window}"
model_env_none = "CLAUDE_CODE_MAX_CONTEXT_TOKENS 未设置"
model_env_set = "env 已设置：CLAUDE_CODE_MAX_CONTEXT_TOKENS={window}（重启 Claude Code 生效；第三方后端可能无效）"
model_env_off = "env 条目已清除"
model_env_bad = "窗口必须是正整数或 off"
model_list_line = "{id}  [{source}]  窗口={window}  usd={usd}  cny={cny}  同步={synced}"
model_src_synced = "已同步"
model_src_user = "手写"
model_src_builtin = "内置"
```

- [ ] **Step 8: 全量测试 + 构建 + 黑盒冒烟**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test` → 全 PASS；`cargo build` → 成功；`target/debug/claude-hud.exe model list` → 输出 11 行合并视图（含 deepseek-v4-flash 窗口=1000000）；`target/debug/claude-hud.exe model env` → 显示当前值或未设置提示；`target/debug/claude-hud.exe --help` → 含 Model registry management 帮助文本。

---

## 任务 8：doctor 扩展（[models] 校验 + 来源信息项）

**Files:**
- Modify: `src/doctor.rs`（model_issues 纯函数 + model_check + run 接线 + tests 模块）
- Modify: `locales/en.toml`、`locales/zh.toml`（runtime.d_model_* 键）

- [ ] **Step 1: 写失败测试（doctor.rs 追加 #[cfg(test)] 模块）**

```rust
#[cfg(test)]
mod tests {
    use super::model_issues;
    use crate::core::config::AppConfig;
    use crate::core::pricing::{ModelEntry, PriceEntry};

    fn cfg_with(entries: Vec<(String, ModelEntry)>) -> AppConfig {
        let mut c = AppConfig::default();
        c.models = entries.into_iter().collect();
        c
    }

    #[test]
    fn model_issues_clean_config() {
        let cfg = cfg_with(vec![(
            "m".into(),
            ModelEntry { context_window: Some(1000), ..Default::default() },
        )]);
        let (bad_window, bad_price) = model_issues(&cfg);
        assert!(bad_window.is_empty() && bad_price.is_empty());
    }

    #[test]
    fn model_issues_zero_window_and_negative_price() {
        let cfg = cfg_with(vec![
            ("a".into(), ModelEntry { context_window: Some(0), ..Default::default() }),
            (
                "b".into(),
                ModelEntry {
                    price_cny: Some(PriceEntry { input: -1.0e-6, ..Default::default() }),
                    ..Default::default()
                },
            ),
        ]);
        let (bad_window, bad_price) = model_issues(&cfg);
        assert_eq!(bad_window, vec!["a".to_string()]);
        assert_eq!(bad_price, vec!["b".to_string()]);
    }

    #[test]
    fn model_issues_ignores_absent_optional_fields() {
        let cfg = cfg_with(vec![("m".into(), ModelEntry::default())]);
        let (bad_window, bad_price) = model_issues(&cfg);
        assert!(bad_window.is_empty() && bad_price.is_empty(), "all-None entry is fine");
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test model_issues`
Expected: FAIL `cannot find function model_issues`。

- [ ] **Step 3: 实现 model_issues + model_check + 接线（doctor.rs）**

pricing_check 后追加：

```rust
/// v0.7 [models] 校验（纯函数，可单测）：返回 (坏窗口模型, 负价模型)。
/// 窗口 ≤0 / 任币种任价格 <0 计入；字段缺失（None）不算。
fn model_issues(config: &AppConfig) -> (Vec<String>, Vec<String>) {
    let bad_window: Vec<String> = config
        .models
        .iter()
        .filter(|(_, m)| m.context_window == Some(0))
        .map(|(id, _)| id.clone())
        .collect();
    let bad_price: Vec<String> = config
        .models
        .iter()
        .filter(|(_, m)| {
            [&m.price_usd, &m.price_cny].iter().flatten().any(|p| {
                p.input < 0.0 || p.output < 0.0 || p.cache_read < 0.0 || p.cache_creation < 0.0
            })
        })
        .map(|(id, _)| id.clone())
        .collect();
    (bad_window, bad_price)
}

/// v0.7 [models] 检查（信息项 + failure）：窗口 ≤0 / 负价 → failure；
/// 信息项：内置表数量 + 各 synced_at 条目来源时间。
fn model_check(config: &AppConfig, lang: Language, failures: &mut usize) {
    let (bad_window, bad_price) = model_issues(config);
    if !bad_window.is_empty() {
        println!(
            "{}",
            tr(lang, "runtime.d_model_window_zero")
                .replace("{models}", &bad_window.join(", "))
        );
        *failures += 1;
    }
    if !bad_price.is_empty() {
        println!(
            "{}",
            tr(lang, "runtime.d_model_price_neg")
                .replace("{models}", &bad_price.join(", "))
        );
        *failures += 1;
    }
    println!(
        "{}",
        tr(lang, "runtime.d_builtin_models")
            .replace("{n}", &crate::core::pricing::builtin_models().len().to_string())
    );
    for (id, m) in &config.models {
        if let Some(ts) = &m.synced_at {
            println!(
                "{}",
                tr(lang, "runtime.d_model_synced").replace("{id}", id).replace("{ts}", ts)
            );
        }
    }
}
```

`run` 中 `pricing_check(config, lang, &mut failures);` 后追加一行：

```rust
    model_check(config, lang, &mut failures);
```

- [ ] **Step 4: 跑测试确认通过 + i18n 键**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test model_issues`
Expected: 3 项全 PASS。

`locales/en.toml` `[runtime]` 追加：

```toml
d_model_window_zero = "[models] window must be > 0: {models}"
d_model_price_neg = "[models] negative price: {models}"
d_builtin_models = "builtin model registry: {n} models"
d_model_synced = "[models] {id} synced from GitHub at {ts}"
```

`locales/zh.toml` `[runtime]` 追加：

```toml
d_model_window_zero = "[models] 窗口必须 > 0：{models}"
d_model_price_neg = "[models] 存在负价：{models}"
d_builtin_models = "内置模型注册表：{n} 个模型"
d_model_synced = "[models] {id} 于 {ts} 从 GitHub 同步"
```

- [ ] **Step 5: 全量回归 + 冒烟**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test` 全 PASS；`cargo build` 后 `target/debug/claude-hud.exe doctor` → 输出含 "builtin model registry: 11 models"（en）信息项；若用户 config 有 `[models."deepseek-v4-flash"]` 则显示 synced 来源行。

---

## 任务 9：serve.rs 币种透传 + web JS 修正

**Files:**
- Modify: `src/serve.rs`（build_api_json 加 currency_symbol 字段；JS 三处硬编码 '$' 改 cur）

- [ ] **Step 1: 写失败测试（serve.rs 文件尾部新建 `#[cfg(test)] mod tests`；若已存在同名模块则并入其中）**

```rust
#[cfg(test)]
mod tests {
    use super::build_api_json;
    use crate::core::config::AppConfig;
    use crate::core::theme::Theme;
    use crate::core::widget::WidgetRegistry;

    #[test]
    fn api_json_includes_currency_symbol() {
        let mut cfg = AppConfig::default();
        cfg.language = "zh".into();
        let registry = WidgetRegistry::new();
        let theme = Theme::default();
        let out = build_api_json(&registry, &cfg, &theme);
        assert!(out.contains("\"currency_symbol\":\"¥\""), "zh → ¥: {}", out);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test api_json_includes_currency_symbol`
Expected: FAIL（JSON 无 currency_symbol 字段）。

- [ ] **Step 3: 实现（serve.rs）**

build_api_json 的 json! 中 `"cost_usd"` 前加一行：

```rust
        "currency_symbol": config.currency(),
```

JS（289 行 `async function refresh()` 内，`const data = await resp.json();` 后加一行）：

```js
    const cur = data.currency_symbol || '$';
```

三处替换：
- 297 行：`document.getElementById('val-cost').textContent = '$' + data.cost_usd.toFixed(4);` → `... = cur + data.cost_usd.toFixed(4);`
- 303 行：`document.getElementById('val-week-cost').textContent = '$' + wk.total_cost.toFixed(2);` → `... = cur + wk.total_cost.toFixed(2);`
- 328 行：`bar.title = d.day + ' $' + d.cost.toFixed(2);` → `bar.title = d.day + ' ' + cur + d.cost.toFixed(2);`

（328 行在 refresh() 内的 forEach 闭包里，`cur` 在作用域内。）

- [ ] **Step 4: 跑测试确认通过 + 冒烟**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test api_json_includes_currency_symbol`
Expected: PASS。`cargo build` 后 `target/debug/claude-hud.exe serve` 手动验证：浏览器 localhost:9527，zh 配置下 Session Cost 显示 ¥。

---

## 任务 10：registry/models.toml 仓库数据文件 + 端到端 + 全量回归

**Files:**
- Create: `registry/models.toml`（仓库注册表，与 builtin_models 同构，随官方调价提交更新）
- Test: 任务 6-7 的 e2e 已覆盖（tiny_http + 注入 prompt）；本任务验证仓库文件可被 parse_registry 接受 + 与内置表一致

- [ ] **Step 1: 建仓库注册表文件**

`registry/models.toml`：

```toml
# claude-hud 模型注册表（官方调价/新模型时提交更新，提交即生效，不必发版）
# 用法：claude-hud model sync
registry_version = "2026-08-05"

[models."claude-opus-4-7"]
context_window = 200000
[models."claude-opus-4-7".price_usd]
input = 5.0e-6
output = 25.0e-6
cache_read = 0.5e-6
cache_creation = 6.25e-6

[models."claude-opus-4-5"]
context_window = 200000
[models."claude-opus-4-5".price_usd]
input = 5.0e-6
output = 25.0e-6
cache_read = 0.5e-6
cache_creation = 6.25e-6

[models."claude-sonnet-4-6"]
context_window = 200000
[models."claude-sonnet-4-6".price_usd]
input = 3.0e-6
output = 15.0e-6
cache_read = 0.3e-6
cache_creation = 3.75e-6

[models."claude-sonnet-4-5"]
context_window = 200000
[models."claude-sonnet-4-5".price_usd]
input = 3.0e-6
output = 15.0e-6
cache_read = 0.3e-6
cache_creation = 3.75e-6

[models."claude-haiku-4-5-20251001"]
context_window = 200000
[models."claude-haiku-4-5-20251001".price_usd]
input = 1.0e-6
output = 5.0e-6
cache_read = 0.1e-6
cache_creation = 1.25e-6

[models."claude-haiku-4-5"]
context_window = 200000
[models."claude-haiku-4-5".price_usd]
input = 1.0e-6
output = 5.0e-6
cache_read = 0.1e-6
cache_creation = 1.25e-6

[models."claude-3-5-sonnet"]
context_window = 200000
[models."claude-3-5-sonnet".price_usd]
input = 3.0e-6
output = 15.0e-6
cache_read = 0.3e-6
cache_creation = 3.75e-6

[models."claude-3-5-haiku"]
context_window = 200000
[models."claude-3-5-haiku".price_usd]
input = 0.8e-6
output = 4.0e-6
cache_read = 0.08e-6
cache_creation = 1.0e-6

[models."claude-3-opus"]
context_window = 200000
[models."claude-3-opus".price_usd]
input = 15.0e-6
output = 75.0e-6
cache_read = 1.5e-6
cache_creation = 18.75e-6

[models."deepseek-v4-flash"]
context_window = 1000000
[models."deepseek-v4-flash".price_usd]
input = 0.14e-6
output = 0.28e-6
cache_read = 0.0028e-6
cache_creation = 0.175e-6
[models."deepseek-v4-flash".price_cny]
input = 1.0e-6
output = 2.0e-6
cache_read = 0.02e-6
cache_creation = 1.25e-6

[models."deepseek-v4-pro"]
context_window = 1000000
[models."deepseek-v4-pro".price_usd]
input = 0.435e-6
output = 0.87e-6
cache_read = 0.003625e-6
cache_creation = 0.54375e-6
[models."deepseek-v4-pro".price_cny]
input = 3.0e-6
output = 6.0e-6
cache_read = 0.06e-6
cache_creation = 3.75e-6
```

- [ ] **Step 2: 仓库文件可解析测试（modelsync.rs tests 追加）**

```rust
    #[test]
    fn repo_registry_file_parses_and_matches_builtin() {
        let content = std::fs::read_to_string("registry/models.toml")
            .expect("registry/models.toml exists");
        let reg = parse_registry(&content).unwrap();
        assert_eq!(reg.models.len(), 11, "9 claude + 2 deepseek");
        let builtin = super::super::pricing::builtin_models();
        for (id, e) in &reg.models {
            let b = builtin.get(id).expect("registry id in builtin");
            assert_eq!(e.context_window, b.context_window, "{id} window");
            assert_eq!(e.price_usd, b.price_usd, "{id} usd");
            assert_eq!(e.price_cny, b.price_cny, "{id} cny");
        }
    }
```

（断言 `e.price_usd == b.price_usd` 需要 `PriceEntry: PartialEq` —— 在 pricing.rs 给 `PriceEntry` 的 derive 追加 `PartialEq`（`#[derive(Debug, Clone, Default, Deserialize, Serialize, PartialEq)]`，一行改动）。`Option<u64>` 与 `Option<String>` 的比较不需要额外 derive。）

- [ ] **Step 3: 跑测试确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test repo_registry_file_parses_and_matches_builtin`
Expected: PASS。

- [ ] **Step 4: 最终全量回归 + 黑盒套件**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test` → 全 PASS；`cargo build` → 成功；`python scripts/test_hud.py` → 黑盒全绿（若套件存在且在本机可跑）。

- [ ] **Step 5: 手工验收清单（用户执行）**

1. `claude-hud model list`：11 行，deepseek-v4-flash 显示 window=1000000、usd=0.14/0.28、cny=1.00/2.00、来源 builtin。
2. `claude-hud model env 1000000` → `claude-hud model env` 显示 =1000000 → `claude-hud model env off` 清除。
3. 临时改 `ANTHROPIC_BASE_URL` 指向测试服务失败场景：`claude-hud model sync` 网络失败 → 报错 exit 1、config.toml 未被改动。
4. 真实 sync（联网）：更新 [models] 带 synced_at；回答 N 不写 env。
5. zh 语言下 status bar 成本段显示 ¥（真实人民币价），en 显示 $。
6. DeepSeek 会话中 context bar 进度条按 1M 窗口显示（~8.9% 而非 44%）。

- [ ] **Step 6: 批次末提交（AskUserQuestion 授权后统一 commit）**

提交内容：任务 1-10 全部源码 + locales + `registry/models.toml`。不 stage：`fixtures/`、`reports/`、`target/`、`target/probe-models.ps1`（临时探测脚本，如不再需要可删除）。提交信息建议：`feat: v0.7 模型能力同步 — 内置模型注册表 + model sync/env/list + 窗口单点解析 + 币种语言感知`。

---

## 规格对照自查（写作时逐条核对）

| 规格条目 | 对应任务 |
|---|---|
| §2 `[models]` 注册表 + ModelEntry + 兼容 `[pricing]` 最高优先 | 任务 1、4 |
| §2 内置表（9 Claude + 2 DeepSeek 双币种） | 任务 2 |
| §2 优先级链 `[pricing]` → `[models]` → 内置 → 透传 | 任务 4（price_for） |
| §3 窗口单点解析 + 5 消费点全覆盖（pct/ETA/告警/通知/serve/dashboard） | 任务 3（管线入口解析，下游自动生效） |
| §4 `model sync` 拉 GitHub 表（HUD_REGISTRY_URL、10s 超时、失败不写、保留用户条目、synced_at） | 任务 6、7 |
| §5 env 用户决定（sync 询问默认 N + `model env` 查看/设置/清除） | 任务 5、7 |
| §6 币种语言感知（currency() 决议 + price_for(lang) + serve 透传 + JS 修正） | 任务 1、4、9 |
| §7 错误降级矩阵（sync 失败 exit 1 不写、doctor 窗口 ≤0/负价 failure） | 任务 6、8 |
| §8 TDD 矩阵（resolve 5 例 / fixture 5 例 / 合并链 / price_for / currency / env / e2e） | 任务 3、6、7、1、4、5、7 |
| §9 命令清单（model sync / env / list + doctor 信息项） | 任务 7、8 |
| §11 明确不做（无探测、无 TTL、无汇率、无自动 env、per-model env） | 无对应任务（刻意） |
