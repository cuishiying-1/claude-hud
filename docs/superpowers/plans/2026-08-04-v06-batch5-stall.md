# 批次 V — 卡顿归因（⑮）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 任务 ⑮ Agent 卡顿归因：每 agent 记录最后工具名，卡顿标记扩展为 `stalled 3m · bash`；无工具记录维持现状。

**Architecture:** 数据层在 transcript.rs 的 ToolUse 归属处同步写 `AgentRecord.last_tool_name`（serde default，旧 state.json 兼容）；显示层抽 `stalled_attr(idle_secs, tool, lang)` 纯函数（复用 agent_detail 既有 `format_dur`），agent_detail 紧凑行卡顿时替换 time 段、alerts dashboard 卡顿行升级归因；i18n 新 key `runtime.stalled_attr`。

**Tech Stack:** Rust 2021 · serde · i18n（locales/en|zh.toml）· 黑盒 harness（scripts/hudlib + test_hud.py）

**用户约束**（本仓库硬性规则）：不自动 `git add/commit/push`（批次末 AskUserQuestion 授权，不带 Co-Authored-By）；不运行 `cargo fmt`；cargo 不在 PATH（命令统一前缀 `export PATH="$HOME/.cargo/bin:$PATH" &&`）；不 stage 未跟踪的 `fixtures/`、`reports/`、`docs/superpowers/`；构建用 `cargo build`（黑盒 harness 从 target/debug/claude-hud.exe 解析）。

---

## 事实基线（本批次全部已验证）

1. **AgentRecord**（transcript.rs:29-48）：`name/model/task_description/start_time_secs/end_time_secs/is_active/last_tool_call_secs/tokens_in/tokens_out/tool_calls`，全部 `#[serde(default)]`——加字段不破坏旧 state.json 反序列化（TranscriptSegment.agents 同理，transcript.rs:530-552 全字段 default）。
2. **工具归属点**（transcript.rs:392-399）：ToolUse 分支内 `if let Some(active) = self.active_recent.clone()` → `agent.last_tool_call_secs = Some(current_secs); agent.tool_calls += 1;` ——最后工具名在此同步写（`name` 变量 355 行已 clone，本分支仍可用）。
3. **AgentRecord 完整字面量**（transcript.rs:403-414，SubagentStart or_insert）：加字段必须补 `last_tool_name: None`；其余构造点（测试）均用 `..Default::default()` 不受影响。
4. **agent_detail 卡顿渲染**（agent_detail.rs:55-95）：`is_stalled()`（需要 timestamps_reliable + now−last_tool > threshold，真实时钟）；紧凑行 `{status} {name} {task} {time}`，time 段显示 elapsed（`format_dur`，agent_detail.rs:32-38 private，60 以上 `XmYs`）；卡顿时 status 是 breathe 动画图标 ◐。
5. **alerts dashboard 卡顿行**（alerts.rs:92-102）：`⚠ Agent 'x' stalled >30s`（stalled_agents(30, now)）；alerts.rs 无测试模块（render_dashboard 无单测，黑盒不测 TUI）。
6. **i18n**：en/zh.toml 段结构，key 扁平点分（`runtime.*`/`widget.*`/`notify.*`）；`tr(lang, key)` + `{placeholder}`。
7. **黑盒 harness**：`render_case(..., config=<TOML 文本>, env_extra=...)`；config 是完整 TOML 文本（write_config 直写 config.toml）；**无 active_mod 时** resolve_compact_layout fallback `config.compact_layout`（compact.rs:73-85）；glacier mod 的 layout=activity（ACTIVITY_WIDGETS 无 agent_detail）→ 断言 agent_detail 必须自定义 compact_layout。`CLAUDE_HUD_PHASE` env 可固定动画相位（既有用例 cases.py:1168/1181 用法）。
8. **fixture**：`fixtures/transcript/timestamps.jsonl` = subagent alpha → Bash → Read → stop（全带 timestamp，最后工具 Read，alpha.last_tool_call_secs 已断言 10:02:00Z）；`agents.jsonl` = 平铺无 timestamp（first_line_has_ts false → timestamps_reliable=false → is_stalled=false，不可靠时间轴不假告警）。**新 fixture `stalled.jsonl`**：subagent_start alpha（无 stop，is_active=true）+ tool_use Bash，全带 timestamp → 可靠时间轴 + 卡顿（now 是真实时钟 2026-08-04，last_tool 2026-07-31 → idle 数天 > 30s）。
9. **黑盒断言约束**：卡顿 idle 依赖真实时钟 → 时长部分不可精确断言（会输出如 `5040m0s`）；断言稳定子串 `stalled` + `· Bash`；固定相位（CLAUDE_HUD_PHASE）按 spec 验收要求注入（断言子串不依赖色码，注入保证确定性渲染）。
10. **现有测试**：transcript.rs:586 断言 `alpha.last_tool_call_secs == Some(10:02:00Z)`（cross_process 测试，可顺带补 last_tool_name）；agent_detail.rs 已有 3 个测试（unreliable ≈ / reliable elapsed / is_stalled 三态），AgentRecord 测试构造用 `..Default::default()`。

---

## 任务 1：AgentRecord.last_tool_name 记录（数据层）

**Files:**
- Modify: `src/core/transcript.rs`（字段 + 归属 + or_insert + 测试）
- Test: `src/core/transcript.rs` tests 模块

- [ ] **Step 1: 写失败测试**

`src/core/transcript.rs` tests 模块追加（用既有 `ts_fixture()` helper）：

```rust
#[test]
fn last_tool_name_attributed_to_active_agent() {
    let mut reader = TranscriptReader::new(ts_fixture());
    reader.read_updates();
    let alpha = reader.agents.get("alpha").expect("alpha parsed");
    assert_eq!(alpha.last_tool_name.as_deref(), Some("Read"));
}
```

- [ ] **Step 2: 运行确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test last_tool_name 2>&1 | tail -10`
Expected: 编译错误 `no field last_tool_name`（RED）。

- [ ] **Step 3: 实现字段 + 归属 + 字面量**

`src/core/transcript.rs` AgentRecord 末尾追加：

```rust
    #[serde(default)]
    pub last_tool_name: Option<String>,
```

ToolUse 归属块（transcript.rs:392-399）追加一行：

```rust
                        if let Some(active) = self.active_recent.clone() {
                            if let Some(agent) = self.agents.get_mut(&active) {
                                agent.last_tool_call_secs = Some(current_secs);
                                agent.last_tool_name = Some(name.clone());
                                agent.tool_calls += 1;
                            }
                        }
```

SubagentStart or_insert 字面量（transcript.rs:403-414）追加：

```rust
                            last_tool_call_secs: None,
                            last_tool_name: None,
```

- [ ] **Step 4: 运行确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test last_tool_name 2>&1 | tail -5`
Expected: PASS。

- [ ] **Step 5: 提交点（批次末统一提交，此处仅记录）**

---

## 任务 2：agent_detail 卡顿归因显示（显示层）

**Files:**
- Modify: `src/widgets/agent_detail.rs`（stalled_attr 纯函数 + render_compact 归因替换 + 测试）
- Modify: `locales/en.toml`、`locales/zh.toml`（`runtime.stalled_attr`）

- [ ] **Step 1: 写失败测试**

`src/widgets/agent_detail.rs` tests 追加：

```rust
    #[test]
    fn stalled_attr_formats_duration_and_tool() {
        // idle 195s = 3m15s；en 文案 `stalled {dur} · {tool}`
        assert_eq!(
            stalled_attr(195, "bash", crate::core::i18n::Language::En),
            "stalled 3m15s · bash"
        );
        assert_eq!(
            stalled_attr(45, "Bash", crate::core::i18n::Language::En),
            "stalled 45s · Bash"
        );
    }

    #[test]
    fn stalled_with_tool_shows_attribution() {
        let mut s = summary(vec![AgentRecord {
            name: "a".into(),
            is_active: true,
            last_tool_call_secs: Some(100),
            last_tool_name: Some("bash".into()),
            ..Default::default()
        }]);
        s.timestamps_reliable = true;
        s.last_event_secs = Some(160);
        let w = AgentDetail::new();
        w.update_transcript(&s);
        let out = w.render_compact(
            &SessionData::default(),
            &Theme::default(),
            &WidgetConfig::default(),
        );
        // idle 依赖真实时钟（now − 100 巨大）→ 只断言稳定子串
        assert!(out.contains("stalled"), "attribution word: {}", out);
        assert!(out.contains("· bash"), "tool attribution: {}", out);
    }

    #[test]
    fn stalled_without_tool_keeps_plain_elapsed() {
        let mut s = summary(vec![AgentRecord {
            name: "a".into(),
            is_active: true,
            start_time_secs: 100,
            last_tool_call_secs: Some(100),
            last_tool_name: None,
            ..Default::default()
        }]);
        s.timestamps_reliable = true;
        s.last_event_secs = Some(160);
        let w = AgentDetail::new();
        w.update_transcript(&s);
        let out = w.render_compact(
            &SessionData::default(),
            &Theme::default(),
            &WidgetConfig::default(),
        );
        // 无工具记录 → 维持现状（elapsed 仍显示，无归因文本）
        assert!(out.contains("1m0s"), "elapsed unchanged: {}", out);
        assert!(!out.contains("stalled"), "no attribution: {}", out);
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test stalled 2>&1 | tail -10`
Expected: 编译错误（`stalled_attr` 不存在；`last_tool_name` 字段在任务 1 后已存在）→ RED。

- [ ] **Step 3: 实现 stalled_attr + i18n**

`src/widgets/agent_detail.rs` format_dur 之后追加（widget 层共享，alerts.rs 复用）：

```rust
/// ⑮ 卡顿归因文本：`stalled {dur} · {tool}`（en）/ `卡顿 {dur} · {tool}`（zh）。
/// idle_secs 是闲置秒数（now − last_tool_call）；时长口径与 format_dur 一致。
pub fn stalled_attr(idle_secs: u64, tool: &str, lang: crate::core::i18n::Language) -> String {
    tr(lang, "runtime.stalled_attr")
        .replace("{dur}", &format_dur(idle_secs))
        .replace("{tool}", tool)
}
```

`locales/en.toml` `[runtime]` 段追加：

```toml
stalled_attr = "stalled {dur} · {tool}"
```

`locales/zh.toml` `[runtime]` 段追加：

```toml
stalled_attr = "卡顿 {dur} · {tool}"
```

- [ ] **Step 4: render_compact 归因替换**

`src/widgets/agent_detail.rs` render_compact：elapsed_str 计算之后、`parts.push` 之前，time 段改按卡顿状态选择：

```rust
                    // ⑮ 卡顿归因：有最后工具名 → time 段替换为归因（danger 色）；
                    // 无工具记录 → 维持现状（elapsed）。
                    let time = if is_stalled {
                        match &agent.last_tool_name {
                            Some(tool) => {
                                let idle = agent
                                    .last_tool_call_secs
                                    .map_or(0, |t| now.saturating_sub(t));
                                ansi::ansi_fg(
                                    &stalled_attr(idle, tool, config.lang),
                                    &theme.danger,
                                )
                            }
                            None => ansi::ansi_fg(&elapsed_str, &theme.muted),
                        }
                    } else {
                        ansi::ansi_fg(&elapsed_str, &theme.muted)
                    };
```

（原 `let time = ansi::ansi_fg(&elapsed_str, &theme.muted);` 删除，替换为上述三态；`now` 变量在 is_stalled 判定处已定义。）

- [ ] **Step 5: 运行确认通过**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test agent_detail 2>&1 | tail -8`
Expected: agent_detail 全部 PASS（既有 3 + 新增 3）。

---

## 任务 3：alerts 行归因 + 黑盒 B5 用例

**Files:**
- Modify: `src/widgets/alerts.rs`（dashboard 卡顿行归因）
- Create: `fixtures/transcript/stalled.jsonl`（新 fixture，不 stage）
- Modify: `scripts/hudlib/cases.py`（b5_cases + 总数断言 165→168）

- [ ] **Step 1: alerts dashboard 行归因**

`src/widgets/alerts.rs` render_dashboard 卡顿循环（alerts.rs:95-99）改为：

```rust
                    for agent in summary.stalled_agents(30, state::now_secs()) {
                        let line_text = match &agent.last_tool_name {
                            Some(tool) => {
                                let idle = agent
                                    .last_tool_call_secs
                                    .map_or(0, |t| now.saturating_sub(t));
                                format!(
                                    "⚠ {} '{}' {}",
                                    tr(config.lang, "runtime.agent_word"),
                                    agent.name,
                                    crate::widgets::agent_detail::stalled_attr(
                                        idle, tool, config.lang
                                    )
                                )
                            }
                            None => format!(
                                "⚠ {} '{}' {} >30s",
                                tr(config.lang, "runtime.agent_word"),
                                agent.name,
                                tr(config.lang, "runtime.stalled")
                            ),
                        };
                        lines.push(Line::from(Span::styled(
                            line_text,
                            Style::default().fg(ansi::parse_ratatui_color(&theme.danger)),
                        )));
                    }
```

（循环内需先取 `let now = state::now_secs();`——若循环外已有同名变量则复用。）

- [ ] **Step 2: 新 fixture**

Create `fixtures/transcript/stalled.jsonl`（两行，全带 timestamp → 可靠时间轴；无 subagent_stop → is_active=true）：

```
{"type":"subagent_start","name":"alpha","model":"deepseek-v4-flash","task":"search","timestamp":"2026-07-31T10:01:00Z"}
{"type":"tool_use","name":"Bash","input":{},"timestamp":"2026-07-31T10:01:30Z"}
```

- [ ] **Step 3: 黑盒 B5 用例**

`scripts/hudlib/cases.py` 在 b4_cases 后追加（config 无 active_mod → compact_layout 生效；agent_detail 已注册 mod.rs:33）：

```python
def b5_cases():
    """批次 V ⑮：卡顿归因。stalled.jsonl → alpha 卡在 Bash（可靠时间轴）。"""
    agent_detail_cfg = (
        "preset = \"full\"\n"
        "separator = \" │ \"\n"
        "compact_layout = [\"agent_detail\"]\n"
        "[dashboard]\nrefresh_interval_ms = 0\ndefault_layout = \"\"\n"
        "[widgets]\n"
    )
    return [
        render_case(
            "B5-01", "⑮ 卡顿归因 en（stalled fixture）", "batch5",
            {"exit": 0, "stdout_contains": ["stalled", "· Bash"]},
            stdin=j(full_dict(**{"model": {"id": "deepseek-v4-flash", "display_name": "DeepSeek"},
                                 "transcript_path": fx("transcript/stalled.jsonl")})),
            config=agent_detail_cfg,
            env_extra={"CLAUDE_HUD_PHASE": "0.25"},
            note="alpha 无 stop 且最后工具 Bash → 归因文本（idle 数天，断言稳定子串）"),
        render_case(
            "B5-02", "⑮ 卡顿归因 zh", "batch5",
            {"exit": 0, "stdout_contains": ["卡顿", "· Bash"]},
            stdin=j(full_dict(**{"model": {"id": "deepseek-v4-flash", "display_name": "DeepSeek"},
                                 "transcript_path": fx("transcript/stalled.jsonl")})),
            config=(
                "language = \"zh\"\n" + agent_detail_cfg
            ),
            env_extra={"CLAUDE_HUD_PHASE": "0"},
            note="zh locale 归因文案"),
        render_case(
            "B5-03", "⑮ 不可靠时间轴不假告警", "batch5",
            {"exit": 0, "stdout_not_contains": ["stalled", "卡顿"]},
            stdin=j(full_dict(**{"model": {"id": "deepseek-v4-flash", "display_name": "DeepSeek"},
                                 "transcript_path": fx("transcript/agents.jsonl")})),
            config=agent_detail_cfg,
            note="agents.jsonl 无 timestamp → timestamps_reliable=false → 无归因"),
    ]
```

CASES 追加 `+ b5_cases()`，总数断言 165 → 168：

```python
# 156 + 3（B1-01..03）+ 1（B2-01）+ 2（B3-01/02）+ 3（B4-01/02/03）+ 3（B5-01/02/03）= 168
assert len(CASES) == 168, f"expected 168 cases, got {len(CASES)}"
```

- [ ] **Step 4: 全量验证**

Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo test 2>&1 | tail -3`
Expected: 全量 PASS（166 + 新增 5 = ~171）。
Run: `export PATH="$HOME/.cargo/bin:$PATH" && cargo build 2>&1 | tail -3 && python scripts/test_hud.py 2>&1 | tail -6`
Expected: 0 warning；黑盒 168/168 PASS。

---

## 收尾

- [ ] **Step 1: 文档同步**
  - `CHANGELOG.md` `[Unreleased]` 追加 1 条 bullet（批次 V 卡顿归因：last_tool_name 记录 + `stalled 3m · bash` 归因标记 + alerts 行升级 + 黑盒 168 例）。
  - `DEPLOY.md`：
    - 「压缩预测」节后或「状态栏宽度」视觉增强行追加卡顿归因说明（`stalled {dur} · {tool}`，无工具记录维持现状；数据来自 transcript 工具归属，不可靠时间轴不显示）。
  - `COMPLETE.md`：✅ 完整实现段追加批次 V；路线图表追加一行；文末时间戳更新。

- [ ] **Step 2: 提交（AskUserQuestion 授权，不带 Co-Authored-By）**
  提交内容：`src/core/transcript.rs`、`src/widgets/agent_detail.rs`、`src/widgets/alerts.rs`、`locales/en.toml`、`locales/zh.toml`、`scripts/hudlib/cases.py`、`CHANGELOG.md`、`DEPLOY.md`、`COMPLETE.md`。
  不提交：`fixtures/`（stalled.jsonl 同既有 fixture 未跟踪保持）、`reports/`、`docs/superpowers/`。

---

## 自审（writing-plans 要求）

- **Spec 覆盖**：⑮ 三点全部覆盖——① last_tool_name 记录（任务 1）；② `stalled 3m · bash` 归因标记（任务 2 agent_detail + 任务 3 alerts 行）；③ 无工具记录维持现状（任务 2 单测 `stalled_without_tool_keeps_plain_elapsed`）。验收"单元测试：工具名记录"（任务 1 Step 1）；"黑盒用例卡顿归因文本（固定相位）"（任务 3 B5-01/02 注入 CLAUDE_HUD_PHASE）。
- **占位符**：无 TBD；所有代码块完整。alerts.rs 循环内 now 变量为「实施时核对」项（已有 `state::now_secs()` 调用点），不阻塞。
- **类型一致性**：`stalled_attr(u64, &str, Language) -> String` 两处调用一致；`last_tool_name: Option<String>` 数据层/显示层一致；i18n key `runtime.stalled_attr` 与既有扁平点分风格一致（注意 NOT `runtime.stalled_attr.x`，无点分冲突风险——[runtime] 段无同名表）。
